title: tengkong resolusi
description: untuk menyimpan tengkong
template:
  repo: tengkong-resolusi-template
  name: tengkong-resolusi
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
